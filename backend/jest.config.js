// jest.config.js
module.exports = {
  testEnvironment: 'node',
  // Add any paths where your test files will be located
  testMatch: ['<rootDir>/__tests__/**/*.test.js'],
  // Or if you prefer to keep them next to the files they test:
  // testMatch: ['<rootDir>/**/*.test.js'],
};